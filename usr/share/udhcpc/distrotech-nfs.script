#!/bin/ash

case ${1} in
        "deconfig")
           /bin/ip link set dev ${interface} up
           ;;
	"bound")
           /bin/ip addr add ${ip}/${subnet} broadcast ${broadcast} dev ${interface}
           /bin/mount -o ro ${router}:/mnt/dev /sysroot
           ;;
esac
